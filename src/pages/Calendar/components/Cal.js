import React, { Component } from "react";
import Calendar from "react-calendar";

class Cal extends Component {
  render() {
    return (
      <div>
        <Calendar />
      </div>
    );
  }
}

export default Cal;
